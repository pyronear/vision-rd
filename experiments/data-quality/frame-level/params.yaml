models:
  yolo11s-nimble-narwhal:
    hf_repo: pyronear/yolo11s_nimble-narwhal_v6.0.0
    hf_filename: best.pt
    conf_thresh: 0.35        # per-detection YOLO confidence
    iou_thresh: 0.05         # FiftyOne evaluate_detections match IoU (very lenient — this is a label audit, not detection eval)
