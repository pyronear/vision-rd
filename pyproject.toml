[project]
name = "pyronear-papers"
version = "0.1.0"
description = "Research papers on temporal ML models for wildfire smoke detection"
requires-python = ">=3.11"
dependencies = [
    "dvc[s3]>=3.56",
]
